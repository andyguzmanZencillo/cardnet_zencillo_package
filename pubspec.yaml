name: cardnet
description: Plugin Flutter para integración con CardNet.
version: 1.1.1
homepage:

environment:
  sdk: ">=3.4.4 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  oxidized: ^6.2.0
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.zencillo.cardnet
        pluginClass: CardNetPlugin
